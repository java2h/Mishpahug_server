package application.repositories;

import application.entities.data.Ishur;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IshurRepository extends JpaRepository<Ishur, Integer> {
}
